services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./invoices.db:/app/invoices.db
      - ./invoices:/app/invoices
    environment:
      - FLASK_ENV=development
    restart: always
